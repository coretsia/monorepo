# Coretsia Framework (Monorepo)
#
# Project: Coretsia Framework (Monorepo)
# Authors: Vladyslav Mudrichenko and contributors
# Copyright (c) 2026 Vladyslav Mudrichenko
#
# SPDX-FileCopyrightText: 2026 Vladyslav Mudrichenko
# SPDX-License-Identifier: Apache-2.0
#
# For contributors list, see git history.
# See LICENSE and NOTICE in the project root for full license information.

name: ci

on:
  push:
    branches: [ "**" ]
  pull_request:

permissions:
  contents: read

jobs:
  gates:
    name: "gates / ubuntu / PHP 8.4"
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout: repository"
        uses: actions/checkout@v6

      - name: "Setup: PHP + Composer"
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer:v2
          coverage: none
          extensions: imagick

      - name: "Guard: managed composer repositories drift (sync:check) (MUST)"
        shell: bash
        run: composer --no-interaction sync:check

      - name: "Install: composer (framework) (install:framework)"
        shell: bash
        run: composer --no-interaction --no-progress install:framework

      - name: "Gate rail: tooling/baseline gates (gates)"
        shell: bash
        run: composer --no-interaction gates

      - name: "Gate rail: DTO aggregate rail (dto:gate)"
        shell: bash
        run: composer --no-interaction dto:gate

      - name: "Guard: lock drift (lock:check) (MUST)"
        shell: bash
        run: composer --no-interaction lock:check

  gates-windows:
    name: "gates / windows / PHP 8.4"
    runs-on: windows-2025-vs2026

    env:
      MSYS: winsymlinks:nativestrict

    steps:
      - name: "Windows rails: git + symlinks (MUST)"
        shell: bash
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf
          git config --global core.symlinks true
          git config --global core.safecrlf true

      - name: "Checkout: repository"
        uses: actions/checkout@v6

      - name: "Windows rails: assert git symlink mode (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          git config core.symlinks true
          test "$(git config --bool core.symlinks)" = "true"

      - name: "Windows rails: verify symlink capability (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .tmp_symlink_check
          mkdir -p .tmp_symlink_check
          echo x > .tmp_symlink_check/target.txt
          ln -s target.txt .tmp_symlink_check/link.txt
          test -L .tmp_symlink_check/link.txt
          rm -rf .tmp_symlink_check

      - name: "Setup: PHP + Composer"
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer:v2
          coverage: none
          extensions: imagick

      - name: "Windows rails: verify PHP symlink capability (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .tmp_symlink_php_check
          mkdir -p .tmp_symlink_php_check
          cd .tmp_symlink_php_check
          echo x > target.txt
          php -r "exit((@symlink('target.txt','link.txt') && is_link('link.txt')) ? 0 : 1);"
          test -L link.txt

      - name: "Windows rails: cleanup PHP symlink check (ALWAYS)"
        if: ${{ always() }}
        shell: bash
        run: |
          rm -rf .tmp_symlink_php_check

      - name: "Guard: managed composer repositories drift (sync:check) (MUST)"
        shell: bash
        run: composer --no-interaction sync:check

      - name: "Install: composer (framework) (install:framework)"
        shell: bash
        run: composer --no-interaction --no-progress install:framework

      - name: "Gate rail: tooling/baseline gates (gates)"
        shell: bash
        run: composer --no-interaction gates

      - name: "Gate rail: DTO aggregate rail (dto:gate)"
        shell: bash
        run: composer --no-interaction dto:gate

      - name: "Guard: lock drift (lock:check) (MUST)"
        shell: bash
        run: composer --no-interaction lock:check

  security:
    name: "security / ubuntu / PHP 8.4"
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout: repository"
        uses: actions/checkout@v6

      - name: "Setup: PHP + Composer"
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer:v2
          coverage: none
          extensions: imagick

      - name: "Setup: Gitleaks"
        shell: bash
        env:
          GITLEAKS_VERSION: "8.30.1"
        run: |
          set -euo pipefail

          install_dir="${RUNNER_TEMP}/gitleaks/bin"
          mkdir -p "${install_dir}"

          archive="${RUNNER_TEMP}/gitleaks.tar.gz"

          curl -fsSL \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" \
            -o "${archive}"

          tar -xzf "${archive}" -C "${install_dir}" gitleaks
          chmod +x "${install_dir}/gitleaks"

          echo "${install_dir}" >> "$GITHUB_PATH"
          "${install_dir}/gitleaks" version

      - name: "Guard: managed composer repositories drift (sync:check) (MUST)"
        shell: bash
        run: composer --no-interaction sync:check

      - name: "Install: composer (framework) (install:framework)"
        shell: bash
        run: composer --no-interaction --no-progress install:framework

      - name: "Security: security aggregate (security)"
        shell: bash
        run: composer --no-interaction security

      - name: "Guard: lock drift (lock:check) (MUST)"
        shell: bash
        run: composer --no-interaction lock:check

  security-windows:
    name: "security / windows / PHP 8.4"
    runs-on: windows-2025-vs2026

    env:
      MSYS: winsymlinks:nativestrict

    steps:
      - name: "Windows rails: git + symlinks (MUST)"
        shell: bash
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf
          git config --global core.symlinks true
          git config --global core.safecrlf true

      - name: "Checkout: repository"
        uses: actions/checkout@v6

      - name: "Windows rails: assert git symlink mode (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          git config core.symlinks true
          test "$(git config --bool core.symlinks)" = "true"

      - name: "Windows rails: verify symlink capability (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .tmp_symlink_check
          mkdir -p .tmp_symlink_check
          echo x > .tmp_symlink_check/target.txt
          ln -s target.txt .tmp_symlink_check/link.txt
          test -L .tmp_symlink_check/link.txt
          rm -rf .tmp_symlink_check

      - name: "Setup: PHP + Composer"
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer:v2
          coverage: none
          extensions: imagick

      - name: "Setup: Gitleaks"
        shell: bash
        env:
          GITLEAKS_VERSION: "8.30.1"
        run: |
          set -euo pipefail

          install_dir="${PWD}/.tmp/gitleaks/bin"
          archive="${PWD}/.tmp/gitleaks/gitleaks.zip"

          rm -rf "${PWD}/.tmp/gitleaks"
          mkdir -p "${install_dir}"

          curl -fsSL \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_windows_x64.zip" \
            -o "${archive}"

          install_dir_win="$(cygpath -w "${install_dir}")"
          archive_win="$(cygpath -w "${archive}")"

          powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \
            "Expand-Archive -LiteralPath '${archive_win}' -DestinationPath '${install_dir_win}' -Force"

          test -f "${install_dir}/gitleaks.exe"

          echo "${install_dir_win}" >> "$GITHUB_PATH"
          "${install_dir}/gitleaks.exe" version

      - name: "Windows rails: verify PHP symlink capability (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .tmp_symlink_php_check
          mkdir -p .tmp_symlink_php_check
          cd .tmp_symlink_php_check
          echo x > target.txt
          php -r "exit((@symlink('target.txt','link.txt') && is_link('link.txt')) ? 0 : 1);"
          test -L link.txt

      - name: "Windows rails: cleanup PHP symlink check (ALWAYS)"
        if: ${{ always() }}
        shell: bash
        run: |
          rm -rf .tmp_symlink_php_check

      - name: "Guard: managed composer repositories drift (sync:check) (MUST)"
        shell: bash
        run: composer --no-interaction sync:check

      - name: "Install: composer (framework) (install:framework)"
        shell: bash
        run: composer --no-interaction --no-progress install:framework

      - name: "Security: security aggregate (security)"
        shell: bash
        run: composer --no-interaction security

      - name: "Guard: lock drift (lock:check) (MUST)"
        shell: bash
        run: composer --no-interaction lock:check

  arch:
    name: "arch / ubuntu / PHP 8.4"
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout: repository"
        uses: actions/checkout@v6

      - name: "Setup: PHP + Composer"
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer:v2
          coverage: none
          extensions: imagick

      - name: "Guard: managed composer repositories drift (sync:check) (MUST)"
        shell: bash
        run: composer --no-interaction sync:check

      - name: "Install: composer (framework) (install:framework)"
        shell: bash
        run: composer --no-interaction --no-progress install:framework

      - name: "Arch: package index generator rerun-no-diff (arch:package-index:check)"
        shell: bash
        run: composer --no-interaction arch:package-index:check

      - name: "Arch: deptrac generator rerun-no-diff (arch:deptrac:check)"
        shell: bash
        run: composer --no-interaction arch:deptrac:check

      - name: "Arch: generate dep graph artifacts (arch:deptrac:generate)"
        shell: bash
        run: composer --no-interaction arch:deptrac:generate

      - name: "Guard: generated arch source drift (MUST)"
        shell: bash
        run: |
          git diff --exit-code -- \
            framework/tools/testing/package-index.php \
            framework/tools/testing/deptrac.yaml \
            framework/tools/testing/deptrac.allowlist.yaml

      - name: "Arch: deptrac analyze (arch:deptrac:analyze)"
        shell: bash
        run: composer --no-interaction arch:deptrac:analyze

      - name: "Upload: dep graph artifacts"
        uses: actions/upload-artifact@v7
        with:
          name: coretsia-deptrac-graph
          path: |
            framework/var/arch/deptrac_graph.dot
            framework/var/arch/deptrac_graph.svg
            framework/var/arch/deptrac_graph.html
          if-no-files-found: error
          retention-days: 14

      - name: "Guard: lock drift (lock:check) (MUST)"
        shell: bash
        run: composer --no-interaction lock:check

  arch-windows:
    name: "arch-check / windows / PHP 8.4"
    runs-on: windows-2025-vs2026

    env:
      MSYS: winsymlinks:nativestrict

    steps:
      - name: "Windows rails: git + symlinks (MUST)"
        shell: bash
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf
          git config --global core.symlinks true
          git config --global core.safecrlf true

      - name: "Checkout: repository"
        uses: actions/checkout@v6

      - name: "Windows rails: assert git symlink mode (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          git config core.symlinks true
          test "$(git config --bool core.symlinks)" = "true"

      - name: "Windows rails: verify symlink capability (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .tmp_symlink_check
          mkdir -p .tmp_symlink_check
          echo x > .tmp_symlink_check/target.txt
          ln -s target.txt .tmp_symlink_check/link.txt
          test -L .tmp_symlink_check/link.txt
          rm -rf .tmp_symlink_check

      - name: "Setup: PHP + Composer"
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer:v2
          coverage: none
          extensions: imagick

      - name: "Windows rails: verify PHP symlink capability (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .tmp_symlink_php_check
          mkdir -p .tmp_symlink_php_check
          cd .tmp_symlink_php_check
          echo x > target.txt
          php -r "exit((@symlink('target.txt','link.txt') && is_link('link.txt')) ? 0 : 1);"
          test -L link.txt

      - name: "Windows rails: cleanup PHP symlink check (ALWAYS)"
        if: ${{ always() }}
        shell: bash
        run: |
          rm -rf .tmp_symlink_php_check

      - name: "Guard: managed composer repositories drift (sync:check) (MUST)"
        shell: bash
        run: composer --no-interaction sync:check

      - name: "Install: composer (framework) (install:framework)"
        shell: bash
        run: composer --no-interaction --no-progress install:framework

      - name: "Arch: package index generator rerun-no-diff (arch:package-index:check)"
        shell: bash
        run: composer --no-interaction arch:package-index:check

      - name: "Arch: deptrac generator rerun-no-diff (arch:deptrac:check)"
        shell: bash
        run: composer --no-interaction arch:deptrac:check

      - name: "Arch: deptrac analyze (arch:deptrac:analyze)"
        shell: bash
        run: composer --no-interaction arch:deptrac:analyze

      - name: "Guard: lock drift (lock:check) (MUST)"
        shell: bash
        run: composer --no-interaction lock:check

  test:
    name: "test / ubuntu / PHP 8.4"
    runs-on: ubuntu-latest
    needs:
      - gates
      - arch

    steps:
      - name: "Checkout: repository"
        uses: actions/checkout@v6

      - name: "Setup: PHP + Composer"
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer:v2
          coverage: none
          extensions: imagick

      - name: "Guard: managed composer repositories drift (sync:check) (MUST)"
        shell: bash
        run: composer --no-interaction sync:check

      - name: "Install: composer (framework) (install:framework)"
        shell: bash
        run: composer --no-interaction --no-progress install:framework

      - name: "Validate: composer (framework) (validate:framework)"
        shell: bash
        run: composer --no-interaction validate:framework

      - name: "Test: framework package/unit/contract suite"
        shell: bash
        run: composer --no-interaction --working-dir=framework run-script test --

      - name: "Guard: lock drift (lock:check) (MUST)"
        shell: bash
        run: composer --no-interaction lock:check

  test-windows:
    name: "test / windows / PHP 8.4"
    runs-on: windows-2025-vs2026
    needs:
      - gates-windows
      - arch-windows

    env:
      MSYS: winsymlinks:nativestrict

    steps:
      - name: "Windows rails: git + symlinks (MUST)"
        shell: bash
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf
          git config --global core.symlinks true
          git config --global core.safecrlf true

      - name: "Checkout: repository"
        uses: actions/checkout@v6

      - name: "Windows rails: assert git symlink mode (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          git config core.symlinks true
          test "$(git config --bool core.symlinks)" = "true"

      - name: "Windows rails: verify symlink capability (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .tmp_symlink_check
          mkdir -p .tmp_symlink_check
          echo x > .tmp_symlink_check/target.txt
          ln -s target.txt .tmp_symlink_check/link.txt
          test -L .tmp_symlink_check/link.txt
          rm -rf .tmp_symlink_check

      - name: "Setup: PHP + Composer"
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer:v2
          coverage: none
          extensions: imagick

      - name: "Windows rails: verify PHP symlink capability (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .tmp_symlink_php_check
          mkdir -p .tmp_symlink_php_check
          cd .tmp_symlink_php_check
          echo x > target.txt
          php -r "exit((@symlink('target.txt','link.txt') && is_link('link.txt')) ? 0 : 1);"
          test -L link.txt

      - name: "Windows rails: cleanup PHP symlink check (ALWAYS)"
        if: ${{ always() }}
        shell: bash
        run: |
          rm -rf .tmp_symlink_php_check

      - name: "Guard: managed composer repositories drift (sync:check) (MUST)"
        shell: bash
        run: composer --no-interaction sync:check

      - name: "Install: composer (framework) (install:framework)"
        shell: bash
        run: composer --no-interaction --no-progress install:framework

      - name: "Validate: composer (framework) (validate:framework)"
        shell: bash
        run: composer --no-interaction validate:framework

      - name: "Test: framework package/unit/contract suite"
        shell: bash
        run: composer --no-interaction --working-dir=framework run-script test --

      - name: "Guard: lock drift (lock:check) (MUST)"
        shell: bash
        run: composer --no-interaction lock:check

  quality:
    name: "quality / ubuntu / PHP 8.4"
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout: repository"
        uses: actions/checkout@v6

      - name: "Setup: PHP + Composer"
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer:v2
          coverage: none
          extensions: imagick

      - name: "Guard: managed composer repositories drift (sync:check) (MUST)"
        shell: bash
        run: composer --no-interaction sync:check

      - name: "Install: composer (framework) (install:framework)"
        shell: bash
        run: composer --no-interaction --no-progress install:framework

      - name: "Quality: code style baseline (cs:check)"
        shell: bash
        run: composer --no-interaction cs:check

      - name: "Quality: static analysis baseline (phpstan)"
        shell: bash
        run: composer --no-interaction phpstan

      - name: "Guard: lock drift (lock:check) (MUST)"
        shell: bash
        run: composer --no-interaction lock:check

  quality-windows:
    name: "quality / windows / PHP 8.4"
    runs-on: windows-2025-vs2026

    env:
      MSYS: winsymlinks:nativestrict

    steps:
      - name: "Windows rails: git + symlinks (MUST)"
        shell: bash
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf
          git config --global core.symlinks true
          git config --global core.safecrlf true

      - name: "Checkout: repository"
        uses: actions/checkout@v6

      - name: "Windows rails: assert git symlink mode (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          git config core.symlinks true
          test "$(git config --bool core.symlinks)" = "true"

      - name: "Windows rails: verify symlink capability (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .tmp_symlink_check
          mkdir -p .tmp_symlink_check
          echo x > .tmp_symlink_check/target.txt
          ln -s target.txt .tmp_symlink_check/link.txt
          test -L .tmp_symlink_check/link.txt
          rm -rf .tmp_symlink_check

      - name: "Setup: PHP + Composer"
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer:v2
          coverage: none
          extensions: imagick

      - name: "Windows rails: verify PHP symlink capability (MUST)"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .tmp_symlink_php_check
          mkdir -p .tmp_symlink_php_check
          cd .tmp_symlink_php_check
          echo x > target.txt
          php -r "exit((@symlink('target.txt','link.txt') && is_link('link.txt')) ? 0 : 1);"
          test -L link.txt

      - name: "Windows rails: cleanup PHP symlink check (ALWAYS)"
        if: ${{ always() }}
        shell: bash
        run: |
          rm -rf .tmp_symlink_php_check

      - name: "Guard: managed composer repositories drift (sync:check) (MUST)"
        shell: bash
        run: composer --no-interaction sync:check

      - name: "Install: composer (framework) (install:framework)"
        shell: bash
        run: composer --no-interaction --no-progress install:framework

      - name: "Quality: code style baseline (cs:check)"
        shell: bash
        run: composer --no-interaction cs:check

      - name: "Quality: static analysis baseline (phpstan)"
        shell: bash
        run: composer --no-interaction phpstan

      - name: "Guard: lock drift (lock:check) (MUST)"
        shell: bash
        run: composer --no-interaction lock:check
